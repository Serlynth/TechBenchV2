# TechBench 0.5.3

- Restores automatic WHD-location to Sage-customer matching after synchronization moved from workstations to the Windows service.
- Runs the established high-confidence matcher after both WHD location snapshots and Sage customer snapshots.
- Automatically applies only mutual, unique, structurally safe matches; ambiguous pairs remain visible for Admin review.
- Adds narrow service-only SQL procedures for reading match candidates and applying row-version-checked merges without granting broad table or Admin access.
- Records automatic merges in the TechBench audit log as `ClientAutoMatched`.

Run the included SQL Server deployment before updating the TechBench server components. The database remains schema version 7, but the service requires the new automatic-matching stored procedures.
