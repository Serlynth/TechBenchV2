# TechBench V2 0.5.108-beta.1

- Fixes startup for authorized users whose Active Directory login name is already attached to an older Windows SID.
- Preserves the historical user row and its related records while registering the authenticated SID as the current identity.
- Keeps the database at schema 15 so existing Stable clients remain compatible.
- Includes the canonical client-profile matching correction from 0.5.107-beta.1.
- Database deployment required: apply the included `TechBenchV2-SQLServer2016-0.5.108-beta.1.sql` to `CSRI-SQL` before the affected user signs in again.
