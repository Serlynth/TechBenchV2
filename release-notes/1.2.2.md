# TechBench 1.2.2

- Adds `Link Existing Sage Ticket...` for reconciling older entries that were manually saved in Sage.
- Keeps an entry Sage pending until the supplied ticket number is confirmed against Sage's committed Time Ticket data through read-only ODBC.
- Uses a styled TechBench prompt for entering the existing Sage ticket number.
- Fixes light-theme header and sidebar text contrast, including muted labels and update status text.
- Continues patch releases by incrementing the final version segment.
