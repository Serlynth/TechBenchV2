# TechBench 0.7.19

- Places AD usernames and passwords directly beside each user in the Client Information migration workbook.
- Places a primary login name, username, and password beside every system, service, network, backup, security, vendor, and unsorted resource row.
- Keeps the Passwords worksheet as the master list for additional or standalone credentials and preserves all previous workbook versions for import.
- Updates the migration workbook to match the current Client Information layout, including separate Backup and Security sheets, category-specific fields, ISP static-address fields, and Needs Sorting.
- Adds Passwords tabs to the user and system editors so linked credentials can be created or updated without leaving the object they belong to.
