# TechBench 0.5.45

- Fixes the client crash introduced when Credentials synchronization progress was added to Admin Center.
- Defers Credentials synchronization status loading until an Admin actually opens Admin Center, so it cannot block workspace startup.
- Contains background session and Credentials-status failures instead of allowing them to close TechBench.
- Keeps the Admin Center **Sync Credentials now** action and progress display.
- Includes the Client WiFi workspace introduced in 0.5.44.
- No SQL schema update is required.
