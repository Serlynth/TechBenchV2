# TechBench 0.5.57

- Adds forward-compatible database handling so the current TechBench client and Server Manager continue working with either schema 13 or schema 14.
- Contains no Inventory Board navigation, database objects, or beta-only behavior.
- Keeps the bundled production SQL installer at schema 13. Apply schema 14 only after every production client and the server package have updated to 0.5.57.
