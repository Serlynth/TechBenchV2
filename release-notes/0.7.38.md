# TechBench 0.7.38

- Fixes the Client Information Users list so clicking a highlighted row always opens the selected-user summary and assigned-device cards.
- Keeps the existing two-way selection binding as a fallback while directly synchronizing the visible Users grid selection with the profile view.

This is a client-only Stable update and remains compatible with TechBench Server 0.6.28.
