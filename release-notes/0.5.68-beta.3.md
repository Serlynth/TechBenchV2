# TechBench Inventory Beta 0.5.68-beta.3

- Replaces technician-column WPF drag/drop with direct captured-pointer reordering.
- Resolves the destination from the column under the pointer, with a tested horizontal-distance fallback.
- Keeps the existing technician drag preview while ensuring mouse release applies and saves the new order.

No SQL schema or Server Manager update is required for this release.
