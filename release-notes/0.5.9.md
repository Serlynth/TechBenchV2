# TechBench 0.5.9

This patch preserves the V0007 read-only Admin-preview contract while keeping
the Credentials workbook path Admin-only. The schema-8 `GetSettings` procedure now
retains the established preview guard expected by the deployment verifier and
adds the server-path filter after that guard.

Version 0.5.9 continues to use SQL schema version 8. Rerun the complete SQL
installer before updating the server and client packages.
