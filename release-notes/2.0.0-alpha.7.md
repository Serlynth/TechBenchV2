# TechBench V2 2.0.0-alpha.7

## Client startup hotfix

- Defines the missing secondary-button resource used by the V1/Google Sheets import controls so the SQL-backed workspace opens successfully.
- Adds a release-blocking XAML resource contract test to detect unresolved named `StaticResource` references before packaging.
- Uses the existing schema-version-6 database and alpha.6 WHD Sync Service; neither requires reinstallation for this client-only update.
