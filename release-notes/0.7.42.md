# TechBench 0.7.42

- Makes the selected Client Information summary a genuinely compact, left-aligned card instead of stretching its border across the page.
- Promotes legacy FireDrill Wi-Fi values into the real Wi-Fi resource: management URL, staff SSID, guest SSID, and access usernames now appear in the selected card and full list.
- Promotes legacy WatchGuard Firebox IP data into the connection resource and WAN IP field so it appears in the selected card and connection table.
- Repairs clients already organized by 0.7.41 as well as previously unlinked legacy rows, without moving passwords out of encrypted credential storage.

Compatible with the existing TechBench Server 0.6.28 package; no SQL or server update is required.
