# TechBench 0.5.70

- Fixes removed Active Directory users remaining visible as Equipment Board technicians.
- Sends the authoritative AD security identifier with each authorized-user snapshot so SQL cleanup does not depend on resolving every account name again.
- Keeps the Server Manager mapping grid aligned with the current AD snapshot and reports reconciliation failures instead of silently showing stale SQL users.

Apply the included SQL Server installer, then update TechBench Server to 0.5.70. Stable and Inventory Beta clients will stop showing retired technicians after their next refresh.
