# TechBench V2 0.5.106-beta.1

- Normalizes the visible TechBench, SalesBench, and AdminBench artwork into the exact same pixel rectangle.
- Removes per-module WPF sizes and offsets so every logo uses one fixed layout path at every display scale.
- Adds real WPF rendering tests at 100%, 125%, 150%, and 200% DPI.
- Makes the local `--shell-demo` mode switchable across all three modules for database-free visual QA.
- Client-only beta update. No server or SQL deployment is required.
