# TechBench V2 0.6.6-beta.14

- Fixes the recovered interface error that appeared when opening the new
  in-place note editor from This Week or History.
- Registers the editor's badge style before WPF creates the deferred editor
  template and adds a regression check for incorrectly ordered local static
  resources.
- Preserves the beta.13 in-place editing and keep-the-ticket-selected behavior.

This is a Client Info Beta client-only hotfix. TechBench Server 0.6.13 and
schema version 15 remain current; no server or SQL update is required.
