# TechBench 0.5.47

- Removes the repeated `Wireless` prefix from field labels displayed in Client WiFi.
- Keeps the original workbook field names internally so filtering and Copy continue to work correctly.
- For example, `Wireless Guest Password` now displays as `Guest Password`.
- Retains the 0.5.46 post-connect stability rollback.
- No SQL schema update is required.
