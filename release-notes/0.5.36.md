# TechBench 0.5.36

- Prevents Client Credentials **Copy** from crashing TechBench when Windows temporarily locks the clipboard.
- Retries clipboard access briefly, then reports a safe error without exposing the credential value.
- Handles a stale or missing dynamic credential-field binding without throwing an unhandled UI exception.
