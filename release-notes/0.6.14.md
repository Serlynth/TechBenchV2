# TechBench Server 0.6.14

- Adds the saved client-folder and legacy Client Info sheet paths used by the
  TechBench `0.7.2` Overview.
- Extends the existing schema-15 deployment additively; no database schema
  version change is required.
- Preserves both paths when duplicate client records are merged.
- Includes the updated standalone SQL deployment, Sync Service package,
  Server Manager, and one-click server installer.
