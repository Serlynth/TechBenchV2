# TechBench 0.5.37

- Moves Client Credentials clipboard operations to a dedicated background STA dispatcher so Copy does not freeze the TechBench UI.
- Avoids the slow persistent clipboard-flush path while keeping the clipboard owner alive for normal paste operations.
- Prevents repeated Copy clicks while one clipboard operation is in progress and retains the non-crashing error handling from 0.5.36.
