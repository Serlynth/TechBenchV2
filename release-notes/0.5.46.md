# TechBench 0.5.46

- Emergency client stability release for the post-connect crash.
- Fully removes the newly added Admin Center Credentials synchronization card, commands, bindings, and polling path from the client.
- Keeps server-owned Credentials synchronization available through TechBench Server Manager.
- Prevents an unexpected WPF interface exception from silently terminating TechBench and records diagnostic details if one occurs.
- Retains the Client WiFi workspace and all Client Credentials functionality.
- No SQL schema update is required.
