# TechBench 1.2.9

- Allows slower Sage workstations up to 20 seconds to open the Time Ticket Note dialog.
- Keeps the wait responsive by polling and proceeds immediately when the dialog appears.
- Continues to send the Note command only once and never saves after a Note timeout.
