# TechBench 0.5.53

- Hides the left navigation scrollbar while preserving mouse-wheel and touch scrolling.
- Replaces the Client Info disclosure chevron with a navigation arrow so the control no longer looks collapsible.
- This client interface update does not require a SQL schema update.
