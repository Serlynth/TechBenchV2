# TechBench V2 0.5.110-beta.1

- Fixes Equipment Board refresh against schema-15 databases that have not yet received the deployed-lifecycle SQL extension.
- Retries Board reads and normal equipment moves using the existing stored-procedure signature when necessary.
- Replaces the raw SQL parameter error with a clear server-update message if Mark Deployed is used before its SQL extension is installed.
- Includes the complete deployed-device workflow from 0.5.109-beta.1.
- Preserves database schema 15 and Stable-client compatibility.
- The Equipment Board works before the SQL update; apply the included `TechBenchV2-SQLServer2016-0.5.110-beta.1.sql` to enable Mark Deployed.
