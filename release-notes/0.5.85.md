# TechBench V2 0.5.85

- Resolves complete client profiles through the existing canonical Client Match when credential workbook names omit words such as “and” or “&”.
- Loads the matched client’s synchronized users and assigned inventory instead of leaving the shorter credential profile disconnected.
- Preserves ambiguity protection: TechBench still refuses to guess when more than one canonical client could match.
- Client-only update. No server or SQL deployment is required.
