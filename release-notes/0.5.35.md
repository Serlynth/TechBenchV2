# TechBench 0.5.35

- Corrects the compiled TechBench Server Manager schema gate to require the current database schema version 12.
- Corrects the legacy PowerShell Server Manager fallback to use the same schema-12 compatibility requirement.
- Removes stale schema-8 and schema-11 checks that could block Credentials and other shared server configuration after a successful schema-12 deployment.
