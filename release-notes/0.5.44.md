# TechBench 0.5.44

- Adds **Client WiFi** directly beneath Client Credentials for every authorized TechBench user.
- Shows only clients that have synchronized workbook columns beginning with `Wireless`.
- Keeps WiFi values masked until the user explicitly reveals the selected client.
- Groups Wireless fields together while leaving the existing Client Credentials workspace intact.
- Uses the existing flexible Credentials synchronization, so no SQL schema update is required.
