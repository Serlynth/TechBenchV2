# TechBench V2 0.7.1

- Makes New Entry client selection respond immediately instead of waiting for the selected client's SQL ticket lookup.
- Loads the client's active tickets asynchronously after the selection is committed.
- Cancels an older ticket request when another client is selected so stale results cannot replace the current client's ticket list.
