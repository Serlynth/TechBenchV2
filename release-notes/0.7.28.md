# TechBench 0.7.28

- Correctly compares workbook passwords with FireDrill flexible fields by using the same client-and-field encryption authenticator used when those fields are stored.
- Distinguishes a missing FireDrill client match from a matched client whose stored values are unavailable.

This release is paired with TechBench Server 0.6.24. Apply the stable 0.6.24 SQL update once on the shared TechBench server, then refresh the existing workbook review.
