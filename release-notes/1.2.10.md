# TechBench 1.2.10

- Reuses tags from saved entries through an **Add saved tag** picker in the note editor.
- Populates the Search tag field with autocomplete suggestions while preserving typed filters.
- Matches complete tags instead of partial substrings and supports requiring multiple tags.
- Normalizes tag spacing and removes case-insensitive duplicates whenever an entry is saved.
