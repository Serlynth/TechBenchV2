# TechBench 0.5.4

- Corrects the SQL Server deployment verifier so schema-version-7 installations recognize the service-only automatic client-matching procedures introduced in 0.5.3.
- Prevents the installer from reporting those narrow EXECUTE grants as unexpected after the database changes were successfully applied.
- Adds regression coverage for forward-compatible V0006 verification on a schema-version-7 database.

This release adds no database migration and remains at schema version 7. Reapply the included SQL Server deployment safely; all deployment batches are idempotent.
