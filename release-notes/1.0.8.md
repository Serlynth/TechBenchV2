# TechBench 1.0.8

- Replaces visible database entry numbers with clear New Entry and Edit Entry headings.
- Uses client names instead of internal entry IDs in normal status and posting messages.
- Keeps the selected Client and Ticket fields populated when an existing note is opened, including tickets that have since closed.
