# TechBench Inventory Beta 0.5.68-beta.2

- Fixes technician-column dragging so dropping anywhere on another technician column always applies the new order.
- Adds a pointer-position fallback for environments where WPF does not deliver the normal drop event.
- Replaces the unsupported visibility font glyph with a consistent vector eye icon.

No SQL schema or Server Manager update is required for this release.
