# TechBench 1.0.11

- Waits for Sage Employee ID, Customer ID, and Activity Item validation to settle instead of reading each field after only 120 milliseconds.
- Re-resolves Sage controls when the Time Tickets form recreates them during customer lookup.
- Re-resolves every required ticket field immediately before Save so final validation never relies on stale native handles.
- Reports the expected and returned field values when Sage rejects a mapping, while leaving the entry pending and unsaved.
