# TechBench 0.5.80

- Fixes Inventory equipment saves failing with a missing or inaccessible
  `tb_FireDrillCredentialKey`.
- Runs Inventory credential encryption and decryption under the database owner
  while continuing to authorize the original signed-in TechBench Admin.
- Adds deployment verification that prevents this SQL permission regression.

Apply the matching 0.5.80 TechBench Server/SQL update to the shared database.
The database schema remains version 15.
