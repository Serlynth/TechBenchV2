# TechBench 0.5.64

- Fixes tickets assigned to Web Help Desk's built-in Helpdesk Manager account not appearing for the mapped TechBench user.
- Organization ticket synchronization now recognizes both the configured `whdmgr` username and WHD's `H. Manager` assignment label.
- The Server Manager mapping label now includes the configured WHD username so the organization-wide account is unambiguous.
- After updating the server, run **Sync all WHD data now** once to repair the existing ticket ownership rows.
