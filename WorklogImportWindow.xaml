<Window x:Class="TechBench.WorklogImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Google Sheets Worklog"
        Height="720"
        Width="1180"
        MinHeight="620"
        MinWidth="960"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <Style TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="RowBackground" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource PanelAltBackgroundBrush}" />
        </Style>
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="{DynamicResource PanelAltBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="8,7" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="{x:Type DataGridRow}">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="Padding" Value="7,5" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Import Worklog" FontSize="24" FontWeight="SemiBold" />
                <TextBlock Text="{Binding FileName}"
                           Foreground="{DynamicResource MutedTextBrush}"
                           TextTrimming="CharacterEllipsis"
                           Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Width="300">
                <TextBlock Text="Duration interpretation"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Margin="1,0,0,7" />
                <ComboBox ItemsSource="{Binding DurationOptions}"
                          SelectedItem="{Binding SelectedDurationOption}"
                          DisplayMemberPath="DisplayName" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1"
                Background="{DynamicResource PanelAltBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,9"
                Margin="0,0,0,12">
            <TextBlock Text="{Binding Summary}" Foreground="{DynamicResource SecondaryTextBrush}" />
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Import" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="62" />
                <DataGridTextColumn Header="Row" Binding="{Binding SourceRowNumber}" IsReadOnly="True" Width="54" />
                <DataGridTextColumn Header="Date" Binding="{Binding WorkDate, StringFormat={}{0:M/d/yyyy}}" IsReadOnly="True" Width="92" />
                <DataGridTextColumn Header="Sheet client" Binding="{Binding SourceClient}" IsReadOnly="True" Width="145" />
                <DataGridTemplateColumn Header="TechBench client" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.Clients, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                      SelectedItem="{Binding SelectedClient, UpdateSourceTrigger=PropertyChanged}"
                                      DisplayMemberPath="DisplayName"
                                      IsTextSearchEnabled="True"
                                      MinWidth="190" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Source" Binding="{Binding SourceDuration}" IsReadOnly="True" Width="70" />
                <DataGridTextColumn Header="Minutes" Binding="{Binding DurationMinutes, UpdateSourceTrigger=PropertyChanged}" Width="78" />
                <DataGridTextColumn Header="Work note" Binding="{Binding Note}" IsReadOnly="True" Width="*" MinWidth="260" />
                <DataGridTextColumn Header="Status" Binding="{Binding MatchLabel}" IsReadOnly="True" Width="125" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Cancel" Width="92" Margin="0,0,8,0" Click="CancelButton_Click" />
                <Button Content="Import Selected"
                        Width="138"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="ImportButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
