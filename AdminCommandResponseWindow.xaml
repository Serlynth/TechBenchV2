<Window x:Class="TechBench.AdminCommandResponseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="720"
        Height="520"
        MinWidth="580"
        MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="{Binding Title}"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       FontSize="25"
                       FontWeight="SemiBold" />
            <TextBlock x:Name="ProgressTextBlock"
                       Text="{Binding ProgressLabel, Mode=OneWay}"
                       Foreground="{DynamicResource AccentBrush}"
                       FontWeight="SemiBold"
                       Margin="0,8,0,0" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="{DynamicResource PanelAltBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="0,16,0,14">
            <StackPanel>
                <TextBlock Text="{Binding Message}"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           TextWrapping="Wrap" />
                <TextBlock Foreground="{DynamicResource MutedTextBrush}"
                           Margin="0,6,0,0">
                    <Run Text="Sent " />
                    <Run Text="{Binding RequestedAtLabel, Mode=OneWay}" />
                </TextBlock>
            </StackPanel>
        </Border>

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Recipients}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource PanelAltBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="14"
                            Margin="0,0,0,9">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.15*" />
                                <ColumnDefinition Width="1.35*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding UserLabel}"
                                           Foreground="{DynamicResource PrimaryTextBrush}"
                                           FontWeight="SemiBold"
                                           FontSize="15" />
                                <TextBlock Text="{Binding MachineLabel, Mode=OneWay}"
                                           Foreground="{DynamicResource MutedTextBrush}"
                                           Margin="0,4,12,0"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="14,0">
                                <TextBlock Text="{Binding Status}"
                                           Foreground="{DynamicResource AccentBrush}"
                                           FontWeight="SemiBold" />
                                <TextBlock Text="{Binding ResponseMessage}"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,4,0,0" />
                            </StackPanel>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding RespondedAtLabel}"
                                       Foreground="{DynamicResource MutedTextBrush}"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Top" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="LiveStatusTextBlock"
                       Text="Listening for responses..."
                       Foreground="{DynamicResource MutedTextBrush}"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Content="Close"
                    Click="CloseButton_Click"
                    Style="{StaticResource SecondaryButtonStyle}"
                    MinWidth="100" />
        </Grid>
    </Grid>
</Window>
